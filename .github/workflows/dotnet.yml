name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore /workspace/actions-runner/_work/dot-net-project-backend/dot-net-project-backend/TeleMedicine_WebService
    - name: Build
      run: dotnet build --no-restore /workspace/actions-runner/_work/dot-net-project-backend/dot-net-project-backend/TeleMedicine_WebService
    - name: Test
      run: dotnet test --no-build --verbosity normal /workspace/actions-runner/_work/dot-net-project-backend/dot-net-project-backend/TeleMedicine_WebService
